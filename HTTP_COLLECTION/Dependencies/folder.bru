meta {
  name: Dependencies
}

docs {
  # Dependencies
  
  These calls allow you to create and view dependencies between service objects. This information is useful when determining what types of ripple effects outages or releases may have when inspecting a service.
  
  
  For more information on the calls, look at the docs in each call
}
