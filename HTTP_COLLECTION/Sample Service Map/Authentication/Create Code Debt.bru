meta {
  name: Create Code Debt
  type: http
  seq: 4
}

post {
  url: {{baseUrl}}/services/:id/debt
  body: json
  auth: inherit
}

params:path {
  id: {{id}}
}

body:json {
  {
    "title": "dotnet 5",
    "description": "service is several major versions behind",
    "type": "code"
  }
}

vars:pre-request {
  id: 
}

assert {
  res.status: eq 201
}

script:pre-request {
  bru.setVar('id', bru.getEnvVar('auth_api_id'))
}
