meta {
  name: Create Release 1 
  type: http
  seq: 9
}

post {
  url: {{baseUrl}}/services/:serviceId/release
  body: json
  auth: none
}

params:path {
  serviceId: id
}

body:json {
  {
    "version": "{{$randomSemver}}",
    "release_date": "{{$randomDateRecent}}"
  }
}

vars:pre-request {
  id: 
}

script:pre-request {
  bru.setVar('id', bru.getEnvVar('warehouse_api_id'))
}
