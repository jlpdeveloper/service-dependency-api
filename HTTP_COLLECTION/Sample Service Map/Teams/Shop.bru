meta {
  name: Shop
  type: http
  seq: 2
}

post {
  url: {{baseUrl}}/teams
  body: json
  auth: inherit
}

body:json {
  {
    "name": "Shop Team"
  }
}

assert {
  res.status: eq 201
}

script:post-response {
  if (res.status == 201) {
    bru.setEnvVar('shop_team_id', res.body, { persist: false})
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
